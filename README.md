# nitinog10/Game-Rise — Documentation

> Auto-generated by [DocuVerse](https://docuverse.ai)

## Overview

The `repo_995944707b7e` repository is a comprehensive web application designed to serve as a platform for gaming enthusiasts. It provides various pages such as `career.html`, `education.html`, `games.html`, `leaderboards.html`, and `tournaments.html` that offer detailed information and interactive features related to gaming careers, educational resources, game listings, leaderboards, and tournaments. The `index.html` serves as the main entry point for users.

The tech stack includes HTML for structuring the web pages, CSS for styling, and JavaScript for client-side interactivity. The server-side logic is handled by `main.py`, which acts as a simple HTTP server to serve static files and manage cross-origin resource sharing (CORS). Additional Python scripts like `csv.py` are used for data manipulation, likely for generating or processing CSV files related to game data or user statistics.

The project utilizes `pyproject.toml` and `poetry.lock` for dependency management, ensuring that the Python environment is consistently set up across different development environments. The `.replit` and `replit.nix` files configure the environment and deployment settings, specifically tailored for the Replit platform.

This repository is intended for developers and gaming enthusiasts who want to create or contribute to a gaming platform that offers comprehensive resources and interactive features.

## Architecture

```markdown
## Architecture

### Overview

The `Game-Rise` project is structured to serve as a simple web application with a backend server and frontend components. The architecture is designed to be modular, allowing for easy maintenance and extension.

### File Structure and Purpose

- **.replit**: Configuration file for the Replit environment, setting up deployment and runtime settings.
- **README.md**: Provides an overview of the project, its structure, and how to get started.
- **career.html**: HTML file for the career page, displaying information about user careers within the game.
- **csv.py**: Python script for handling CSV file operations, likely used for data processing or storage.
- **education.html**: HTML file for the education page, providing educational content related to the games.
- **games.html**: HTML file for the games page, listing available games and providing links to play.
- **generated-icon.png**: Image file, likely used as a favicon or icon for the web application.
- **index.html**: The main entry point for the web application, serving as the homepage.
- **leaderboards.html**: HTML file for the leaderboards page, displaying rankings for various games.
- **main.py**: The main Python script that serves as the entry point for the HTTP server, handling requests and serving static files.
- **poetry.lock**: Lock file for dependency management using Poetry, ensuring consistent dependency versions.
- **pyproject.toml**: Configuration file for the project, including dependency specifications and project metadata.
- **replit.nix**: Nix module for setting up project dependencies and environment on Replit.
- **script.js**: JavaScript file containing client-side scripts for dynamic behavior on the web pages.
- **styles.css**: CSS file for styling the web application, ensuring a consistent look and feel across pages.
- **tournaments.html**: HTML file for the tournaments page, displaying information about ongoing and upcoming tournaments.

### Data Flow

1. **User Interaction**: Users interact with the web application through the browser, navigating between pages (`index.html`, `games.html`, `leaderboards.html`, etc.).
2. **Static File Serving**: When a user requests a page, `main.py` serves the corresponding HTML, CSS, and JavaScript files.
3. **Dynamic Content**: JavaScript (`script.js`) handles dynamic interactions on the client side, such as updating the leaderboard or fetching game data.
4. **Data Processing**: Python scripts like `csv.py` process data, possibly reading from or writing to CSV files for storage or manipulation.
5. **Display**: Processed data is displayed on the relevant HTML pages, styled with `styles.css`.

### Key Design Patterns

- **MVC (Model-View-Controller)**: The application loosely follows an MVC pattern where:
  - **Model**: Represented by Python scripts (`csv.py`) handling data.
  - **View**: HTML files (`index.html`, `games.html`, etc.) rendering the UI.
  - **Controller**: JavaScript (`script.js`) managing user interactions and dynamic content updates.

### Main Entry Points

- **`main.py`**: The primary entry point for the server, responsible for handling HTTP requests and serving static files.
- **`index.html`**: The main entry point for users, serving as the homepage of the web application.

### Practical Notes for Developers

- **Environment Setup**: Ensure dependencies are installed using `poetry install`.
- **Running the Server**: Execute `python main.py` to start the HTTP server.
- **Frontend Changes**: Modify HTML, CSS, and JavaScript files directly to update the UI.
- **Backend Logic**: Add or modify Python scripts for data processing and server-side logic.
- **Deployment**: Use the `.replit` and `replit.nix` files to configure and deploy the application on Replit.
```

## Folder Structure

```
├── .replit
├── README.md
├── career.html
├── csv.py
├── education.html
├── games.html
├── generated-icon.png
├── index.html
├── leaderboards.html
├── main.py
├── poetry.lock
├── pyproject.toml
├── replit.nix
├── script.js
├── styles.css
└── tournaments.html
```

## Dependencies

## Dependencies

### Production Dependencies

- **pyttsx3** (`^2.98`)
  - Purpose: A text-to-speech conversion library that works offline.
- **speechrecognition** (`^3.11.0`)
  - Purpose: A library for performing speech recognition, converting audio to text.
- **wikipedia** (`^1.4.0`)
  - Purpose: A library for accessing Wikipedia data.

### Development Dependencies

- **python** (`>=3.10.0,<3.12`)
  - Purpose: The programming language runtime.

### Build System

- **poetry-core** (`>=1.0.0`)
  - Purpose: A core library for Poetry, used for building and managing the project.

### Linting and Type Checking

- **pyright**
  - Configuration: Uses library code for types and excludes `.cache` directory.
- **ruff**
  - Configuration: Selects various linting rules and ignores specific warnings (`W291`, `W292`, `W293`).

## File Documentation

### .replit

#### Overview

# **replit** Module Overview

This file configures the environment and deployment settings for the project when running on Replit. It specifies the entry point, required modules, deployment targets, and workflow configurations.

#### Dependencies

- **None**: This file does not import any external modules.

#### Configuration

| Key            | Value                  | Description                                                                                     |
|----------------|------------------------|-------------------------------------------------------------------------------------------------|
| entrypoint     | "main.py"              | The main Python script that serves as the entry point for the application.                       |
| modules        | ["python-3.11"]         | Specifies the required Python module version.                                                    |
| channel        | "stable-24_05"         | Nix channel to use for package management.                                                      |
| unitTest       | "python3"              | Language for unit tests.                                                                       |
| gitHubImport   | {"requiredFiles": [".replit", "replit.nix"]} | Specifies required files for GitHub import.                                                     |
| deployment     | {"run": ["python3", "main.py"], "deploymentTarget": "cloudrun"} | Specifies the command to run the application and the deployment target.                         |
| workflows      | {"runButton": "Run Server"} | Configuration for the workflow button to run the server.                                        |
| workflows.workflow | {"name": "Run Server", "author": 37324147} | Details about the workflow named "Run Server" created by user 37324147.                        |
| workflows.workflow.tasks | [{"task": "shell.exec", "args": "python main.py"}] | Task to execute the main Python script via shell.                                               |
| ports          | {"localPort": 8080, "externalPort": 80} | Local and external port mappings for the application.                                           |

#### Notes

- The `entrypoint` is crucial for defining the main script that initializes the application.
- The `deployment` section specifies that the application should be run using Python 3 and deployed to Cloud Run.
- The `workflows` section configures the "Run Server" button to execute the main script.
- Ensure that `main.py` is correctly set up to handle the application's entry point and dependencies.

### README.md

#### Overview

# Module Overview

This file provides an overview of the `Game-Rise` project, detailing its structure, dependencies, and key components. It serves as a quick reference for developers to understand the project's setup and functionality.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `pygame` | Handles game graphics and user input. |
| `random` | Provides functions for generating random numbers. |
| `sys` | Allows interaction with the interpreter and system-specific parameters. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `Game` | Manages the game loop and state. | `start()`, `update()`, `render()` |
| `Player` | Represents the player character. | `move()`, `jump()` |
| `Enemy` | Represents non-player characters. | `chase()`, `attack()` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `load_assets()` | `directory` | `assets` | Loads game assets from a specified directory. |
| `generate_level()` | `level_number` | `level_map` | Generates a level map based on the provided level number. |

#### Configuration

| Variable | Purpose |
| --- | --- |
| `SCREEN_WIDTH` | Sets the width of the game screen. |
| `SCREEN_HEIGHT` | Sets the height of the game screen. |

#### Notes

- Ensure all game assets are placed in the `assets` directory for `load_assets()` to function correctly.
- The `start()` method in the `Game` class should be called to initiate the game loop.
- Random number generation in `generate_level()` can lead to varied gameplay experiences.

### career.html

#### Overview

# career.html Documentation

#### Module Overview

This file displays the "Career Paths" page, showcasing various career options within the esports industry. It's a static HTML page styled with CSS.

#### Dependencies

- `styles.css`: Custom styles for the page.
- `https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css`: Font Awesome icons.
- `https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap`: Poppins font.

#### Classes

None. This is a static HTML file without any JavaScript classes.

#### Functions

None. This is a static HTML file without any JavaScript functions.

#### Configuration

None. This is a static HTML file without any configuration settings.

#### Constants

None. This is a static HTML file without any constants.

#### Notes

- The page uses Font Awesome icons for visual elements.
- Custom CSS animations are applied to certain elements for a better user experience.
- The page is responsive, adapting to different screen sizes.

### csv.py

#### Overview

# csv.py

#### Module Overview

This file defines the `GameRiseApp` class, which is the main application window for the GameRise platform. It sets up the GUI using `tkinter` and organizes the interface into several tabs, each representing a different section of the platform.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `tkinter` | Provides the GUI toolkit for building the application window and widgets. |
| `ttk` | Offers themed widgets for a consistent look and feel. |
| `messagebox` | Used for displaying message dialogs to the user. |
| `filedialog` | Facilitates file selection dialogs. |
| `os` | Provides a way to interact with the operating system. |
| `csv` | Enables reading from and writing to CSV files. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `GameRiseApp` | Main application window for the GameRise platform. | `__init__`, `add_visual_effect` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `GameRiseApp.__init__` | `self` | None | Initializes the application window and sets up the GUI components. |
| `GameRiseApp.add_visual_effect` | `self` | None | Adds a visual effect to the title label. |

#### Notes

- The `add_visual_effect` method is a placeholder and should be replaced with actual visual effects.
- The tabs currently contain placeholder labels and should be replaced with actual content.
- Ensure that the `csv` module is used appropriately for any CSV file operations required by the application.

### education.html

#### Overview

# education.html Documentation

#### Module Overview

Displays the education section of the GameRise platform, showcasing various gaming courses.

#### Dependencies

- `styles.css`: Custom styles for the GameRise platform.
- `https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css`: Font Awesome icons.
- `https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap`: Poppins font.
- `script.js`: Custom JavaScript for the platform.

#### Classes

None

#### Functions

None

#### Configuration

None

#### Constants

None

#### Notes

- The course data is hardcoded in the HTML. For a dynamic solution, consider fetching this data from a backend API.
- The `enroll-btn` buttons currently do not have any functionality. You'll need to add JavaScript event listeners to handle enrollment logic.
- The platform uses CSS variables for colors and other styles. These are defined in `styles.css`.

### games.html

#### Overview

# games.html Documentation

#### Module Overview

Displays a curated list of top esports games, including images, descriptions, and difficulty levels. Users can click on a game to view a detailed guide.

#### Dependencies

- `styles.css`: Custom styles for the page.
- `https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css`: Icon library.
- `https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap`: Custom font.

#### Classes

None. This is a static HTML file without any JavaScript classes.

#### Functions

None. This is a static HTML file without any JavaScript functions.

#### Configuration

None. This is a static HTML file without any configuration options.

#### Constants

None. This is a static HTML file without any constants.

#### Notes

- The `data-delay` attribute on `.game-large-card` elements is used for staggered reveal animations but is not implemented in this snippet.
- The `view-guide-btn` links are placeholders and do not lead to actual guides.
- The last game card uses a placeholder image.

### index.html

#### Overview

# `index.html` Documentation

#### Module Overview

The `index.html` file is the main entry point for the GameRise website. It defines the structure and content of the homepage, including the header, hero section, feature cards, and mobile navigation.

#### Dependencies

- `styles.css`: Custom styles for the website.
- `https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css`: Font Awesome icons.
- `https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap`: Poppins font.

#### Configuration

- Meta tags for character set, viewport settings, and title.
- CSS variables for colors and other styles.

#### Notes

- The `reveal-on-scroll` class is used for scroll-based animations but is not defined in this file. Ensure it's included in `styles.css`.
- The `pulse-animation` and `floating-3d` classes are custom animations applied to the logo in the hero section.
- The `mobile-nav` section is intended for mobile users and should be hidden on desktop views using media queries in `styles.css`.

### leaderboards.html

#### Overview

# Module Overview
Displays the leaderboards page for various games on the GameRise platform.

#### Dependencies

- `styles.css`: Custom styles for the GameRise platform.
- `https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css`: Font Awesome icons.
- `https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap`: Poppins font.

#### Notes

- The leaderboard data is hardcoded for demonstration purposes. In a real-world scenario, this would be dynamically fetched from a backend service.
- The active tab in the leaderboard tabs is determined by the class `active`. This should be dynamically set based on user interaction or other logic.

### main.py

#### Overview

# Module Overview

`main.py` is the entry point for a simple HTTP server that serves static files with cross-origin resource sharing (CORS) enabled. It uses Python's built-in HTTP server capabilities to handle web requests.

# Dependencies

- `http.server`: Provides classes for implementing web servers.
- `SimpleHTTPRequestHandler`: A basic HTTP request handler that serves files from the current directory.
- `os`: Provides a way to use operating system dependent functionality like reading or even writing to environment variables.

# Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `CustomHandler` | Custom HTTP request handler that adds CORS headers | `end_headers()` |

# Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `run` | `server_class`, `handler_class`, `port` | None | Starts the HTTP server with the specified server class, handler class, and port |

# Notes

- The `CustomHandler` class overrides the `end_headers` method to add a CORS header before calling the parent class's method.
- The server runs on all network interfaces (`0.0.0.0`) on port `8080` by default.
- This server is intended for development purposes and should not be used in production without additional security measures.

### pyproject.toml

#### Overview

# pyproject.toml

#### Module Overview

This file configures the build and dependency management for our project using Poetry. It specifies the project name, version, dependencies, and tools for type checking and linting.

#### Dependencies

| Dependency | Purpose |
|------------|---------|
| `pyttsx3` | Text-to-speech engine. |
| `speechrecognition` | Speech recognition library. |
| `wikipedia` | Wikipedia API client. |

#### Configuration

### Tool: Poetry

| Setting | Purpose |
|---------|---------|
| `name` | Project name. |
| `version` | Project version. |
| `authors` | List of authors. |

### Tool: Pyright

| Setting | Purpose |
|---------|---------|
| `useLibraryCodeForTypes` | Uses library code for type information. |
| `exclude` | Directories to exclude from type checking. |

### Tool: Ruff

| Setting | Purpose |
|---------|---------|
| `select` | Linting rules to enforce. |
| `ignore` | Linting rules to ignore. |

#### Notes

- Ensure that the Python version specified in `[tool.poetry.dependencies]` is compatible with the project requirements.
- The `exclude` path in `tool.pyright` should be updated if additional directories need to be excluded from type checking.
- Linting rules in `tool.ruff` can be adjusted based on team preferences and project standards.

### replit.nix

#### Overview

# replit.nix

**Module Overview**

This file defines a Nix module for setting up dependencies required for the project. It imports `espeak-ng`, a text-to-speech engine, to provide speech synthesis capabilities.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `pkgs.espeak-ng` | Adds the `espeak-ng` package for text-to-speech functionality. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `deps` | None | List of packages | Specifies the list of dependencies required for the project. |

#### Notes

- Ensure that `espeak-ng` is correctly installed and configured for speech synthesis to work as expected.
- This module is crucial for environments where text-to-speech functionality is required.

### script.js

#### Overview

# script.js Documentation

#### Module Overview

This file handles various client-side interactions and animations for the website. It adds particle effects, manages the mobile menu, handles navigation, implements smooth scrolling, animates elements on scroll, and provides modal functionality for sign-in and join actions.

#### Dependencies

- **DOM API**: For manipulating the document and handling events.
- **CSSOM API**: For styling elements dynamically.

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `animateOnScroll` | None | None | Animates elements with specific classes when they come into view on scroll. |

#### Configuration

- **`navLinks`**: An object mapping navigation link identifiers to their corresponding URLs.

#### Constants

None

#### Notes

- The `animateOnScroll` function is called both on scroll events and once on page load to ensure elements are animated immediately.
- Modals are dynamically created and appended to the DOM when sign-in or join buttons are clicked. They are removed when the close button is clicked.
- Navigation links with an `href` starting with `#` are handled to either scroll smoothly to the target or redirect to a specified page based on the `navLinks` configuration.

### styles.css

#### Overview

# styles.css Documentation

#### Module Overview

This CSS file defines the global styles, layout, and animations for the entire application. It includes base styles, component-specific styles, and responsive design adjustments.

#### Dependencies

- `Poppins` font from Google Fonts

#### Classes

| Class               | Purpose                                                                                       |
|---------------------|----------------------------------------------------------------------------------------------|
| `:root`             | Defines CSS variables for colors, gradients, transitions, and shadows                        |
| `*`                 | Resets margin, padding, and sets box-sizing to border-box for all elements                    |
| `body`              | Sets global font, background color, text color, line height, and hides horizontal overflow   |
| `a`                 | Removes text decoration and sets transition for links                                        |
| `button`            | Sets cursor, removes border and outline, sets font and transition for buttons                 |
| `ul`                | Removes list style for unordered lists                                                       |
| `header`            | Styles the header with flexbox, padding, background color, and sticky positioning            |
| `.logo`             | Styles the logo container with flexbox and gap                                               |
| `.logo i`           | Sets color and font size for logo icons                                                      |
| `.logo h1`          | Styles the logo text with gradient background and transparent color                          |
| `nav ul`            | Styles the navigation menu with flexbox, background color, border radius, padding, and shadow|
| `nav ul li a`       | Styles navigation links with padding, border radius, transition, and flexbox                 |
| `nav ul li a:hover` | Changes color and background on hover for navigation links                                   |
| `nav ul li a::after`| Adds underline effect with gradient on hover for navigation links                            |
| `nav ul li a.active`| Styles active navigation link with accent color and gradient background                      |
| `.auth-buttons`     | Styles container for authentication buttons with flexbox and gap                             |
| `.sign-in`          | Styles sign-in button with transparent background, border, and hover effects                |
| `.join`             | Styles join button with gradient background and hover effects                               |
| `.menu-toggle`      | Styles menu toggle button (hidden by default)                                                |
| `.hero`             | Styles the hero section with flexbox, background image, and gradient overlay                 |
| `.hero::after`      | Adds gradient overlay to the hero section                                                    |
| `.hero-content`     | Container for hero content with max-width and z-index                                        |
| `.hero-content h1`  | Styles hero heading with gradient text and animation                                         |
| `.hero-content p`   | Styles hero paragraph with color, margin, and animation                                      |
| `.cta-buttons`      | Styles container for call-to-action buttons with flexbox and gap                             |
| `.start-journey`    | Styles start journey button with gradient background and hover effects                       |
| `.explore-games`    | Styles explore games button with transparent background and hover effects                    |
| `.mobile-nav`       | Styles mobile navigation (hidden by default) with fixed positioning and background color     |
| `.nav-icons`        | Styles container for navigation icons with flexbox, justify-content, and align-items         |
| `.nav-icons a`      | Styles navigation icons with flexbox, font size, and color                                   |
| `.nav-icons a i`    | Styles icon elements within navigation links                                                |
| `.nav-icons a:hover i`| Changes icon color on hover for navigation links                                           |
| `section`           | Sets padding for all sections                                                                |
| `.section-header`   | Styles section header with flexbox, justify-content, align-items, and margin-bottom          |
| `.section-header h2`| Styles section heading with font size and weight                                             |
| `.view-all`         | Styles view all link with color, font weight, flexbox, align-items, and gap                  |
| `.view-all:hover`   | Changes color on hover for view all link                                                     |
| `.games-grid`       | Styles games grid with CSS grid layout, columns, and gap                                      |
| `.game-card`        | Styles game card with border radius, overflow, transition, and height                        |
| `.game-card:hover`  | Adds hover effect for game card                                                              |
| `.game-image`       | Styles game image with width, height, background size, and position                          |
| `.overlay`          | Styles overlay for game card with position, padding, and gradient background                 |
| `.overlay h3`       | Styles overlay heading with font size and weight                                             |
| `.overlay p`        | Styles overlay paragraph with color and font size                                            |
| `.tournament-grid`  | Styles tournament grid with CSS grid layout, columns, and gap                                |
| `.tournament-card`  | Styles tournament card with background color, border radius, overflow, and transition        |
| `.tournament-card:hover`| Adds hover effect for tournament card                                                      |
| `.tournament-image` | Styles tournament image with height, background size, and position                           |
| `.tournament-date`  | Styles tournament date container with position, background color, color, padding, and border radius |
| `.day`              | Styles day element within tournament date with display, font size, and weight               |
| `.month`            | Styles month element within tournament date with display, font size, and text transform     |
| `.tournament-info`  | Styles tournament info container with padding                                               |
| `.tournament-info h3`| Styles tournament info heading with font size and margin-bottom                             |
| `.game`             | Styles game element within tournament info with color and font size                         |
| `.tournament-details`| Styles tournament details container with flexbox, justify-content, and color               |
| `.tournament-details i`| Styles icon element within tournament details with margin-right                           |
| `.register-btn`     | Styles register button with width, gradient background, color, padding, border radius, and font weight |
| `.register-btn:hover`| Adds hover effect for register button                                                     |
| `.roadmap`          | Styles roadmap section with background color                                                 |
| `.roadmap-container`| Styles roadmap container with flexbox, justify-content, gap, and flex-wrap                  |
| `.roadmap-phase`    | Styles roadmap phase container with flex, min-width, background color, border radius, padding, and position |
| `.phase-number`     | Styles phase number container with position, width, height, background, border radius, display, justify-content, align-items, font weight, and font size |
| `.phase-content h3`| Styles phase content heading with margin-bottom and font size                                |
| `.phase-content p`  | Styles phase content paragraph with color and margin-bottom                                 |
| `.phase-content ul`| Styles phase content unordered list with padding-left                                       |
| `.phase-content ul li`

### tournaments.html

#### Overview

# tournaments.html Documentation

#### Module Overview

This file renders the "Tournaments" page for the GameRise platform, displaying a grid of ongoing and upcoming tournaments.

#### Dependencies

- `styles.css`: Custom styles for the GameRise platform.
- `https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css`: Font Awesome icons.
- `https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap`: Poppins font.
- `script.js`: JavaScript file containing event listeners and other interactive behaviors.

#### Configuration

- The file uses CSS variables for colors and other styles, defined in `styles.css`.

#### Notes

- The "Register" buttons in each tournament card currently do nothing. They are placeholders for future functionality.
- The tournament data is hardcoded for now. We'll switch to fetching this from a backend API in a future release.

